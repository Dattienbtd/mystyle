<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <declare-styleable name="CropperView">
        <attr name="grid_color" format="color" />
        <attr name="grid_thickness" format="dimension" />
        <attr name="grid_opacity" format="float" />
        <attr name="padding_color" format="color" />
        <attr name="add_padding_to_make_square" format="boolean" />
    </declare-styleable>

    <style name="progress_dialog_stule" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>


    <style name="ProgressHUD" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="styleButtonLogin">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp50</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_marginEnd">@dimen/dp60</item>
        <item name="android:layout_marginLeft">@dimen/dp60</item>
        <item name="android:layout_marginRight">@dimen/dp60</item>
        <item name="android:layout_marginStart">@dimen/dp60</item>
        <item name="android:background">@drawable/bg_btn</item>
        <item name="android:layout_marginTop">@dimen/dp15</item>
    </style>

    <style name="styleImageLogin">
        <item name="android:layout_width">@dimen/dp25</item>
        <item name="android:layout_height">@dimen/dp25</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:layout_marginLeft">@dimen/dp20</item>
        <item name="android:layout_marginStart">@dimen/dp20</item>
    </style>

    <style name="styleTextLogin">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">center|left</item>
        <item name="android:layout_marginLeft">@dimen/dp55</item>
        <item name="android:layout_marginStart">@dimen/dp55</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/sp16</item>
    </style>


</resources>
